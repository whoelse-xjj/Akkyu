using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using System.Linq;

public class GardenScript : MonoBehaviour
{
    //public int Count;

    //public List<Transform> Flowers;

    //private void OnMouseDown()
    //{
    //    Vector3 position = Camera.main.ScreenToWorldPoint(Input.mousePosition);
    //    Transform flower = Flowers[Random.Range(0, Flowers.Count)];
    //    Instantiate(flower, new Vector3(position.x, position.y, -0.1F), flower.transform.rotation, transform);

    //    Count = transform.childCount;
    //    Transform[] children = GetComponentsInChildren<Transform>();
    //    var message = (from c in children
    //                   where c.name != transform.name
    //                  group c by c.name into g
    //                  select name + g.Count()).ToArray();

    //    foreach (var m in message)
    //    {
    //        Debug.Log(m);
    //    }
    //}
}   
